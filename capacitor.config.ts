import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ATV-GERENCIADOR-DE-TAREFAS',
  webDir: 'www'
};

export default config;
